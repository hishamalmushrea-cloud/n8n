# 🔗 ربط وكيل Arena بـ Blender — أوتوماتيك بالكامل

قلت: «صمم لي بيتاً» → **تراه يُبنى مباشرة في نافذة Blender أمامك** → أعرض عليك الصورة النهائية هنا في المحادثة، وأعدّل حتى تعجبك.

```
أنت تكتب هنا «صمم بيت»
        │
        ▼
وكيل Arena يكتب كود التصميم ──▶ مستودع GitHub ◀── يسحب/يرفع وكيل المزامنة كل 10 ثوانٍ
        ▲                                           │
        │                                           ▼
   أرى الصورة وأعرضها عليك ◀── رفع النتيجة     Blender عندك:
   وأعدّل حسب طلبك                        الكائنات تظهر LIVE في النافذة
                                          + صورة رندر تُحفظ تلقائياً
```

## ⚙️ التجهيز — مرة واحدة فقط (5 دقائق) ثم تنسى الأمر للأبد

### المتطلبات (إن لم تكن مثبتة): [git](https://git-scm.com/download/win) + [Python](https://python.org/downloads) (فعّل Add to PATH) + Blender

### الخطوات
1. **نزّل ملفاً واحداً فقط** من المستودع:
   [`arena-bridge/setup-windows.cmd`](https://github.com/hishamalmushrea-cloud/n8n/raw/arena/01a05efe-n8n/arena-bridge/setup-windows.cmd)
   (زر Raw ← Save As — أو أي طريقة تنزيل)
2. **انقر عليه مرتين** — سيجهّز كل شيء ويطبع لك التعليمات
3. **ثبّت الإضافة في Blender** (مرة واحدة): `Edit ← Preferences ← Add-ons ← Install`
   واختر: `C:\Users\أنت\arena-link\arena-bridge\blender_bridge_addon.py` ثم فعّلها ✅
   > منذ الآن: **كل مرة تفتح Blender يشتغل الجسر تلقائياً** — لا تشغّل شيئاً يدوياً
4. **شغّل الوكيل**: انقر مرتين على `C:\Users\أنت\arena-link\arena-bridge\start-link.cmd`
   > اختياري: انقر `install-autostart.cmd` مرة واحدة → الوكيل يعمل تلقائياً مع تشغيل الويندوز — **صفر نقرات بعدها أبداً**
   > عند أول رفع يسألك git عن حساب GitHub — سجّل دخولك (يُحفظ للأبد)

### انتهى التجهيز ✅
من هذه اللحظة: افتح بلندر متى شئت، واكتب لي هنا ما تريد — لا ملفات، لا نسخ، لا أوامر.

## 🧪 جرّب الآن
اكتب لي في المحادثة:
- **«اختبر الرابط»** — أفحص الاتصال ببلندرك وأخبرك بالنتيجة
- **«صمم لي بيتاً بغرفتين ونوافذ ورندره»** 🏠
- **«اعمل إعلاناً لمنتجي (وصف المنتج)»** 📦

ستلاحظ: الكائنات تظهر وتبنى في نافذة بلندر أمامك مباشرة، وبعد ثوانٍ أعرض لك صورة الرندر هنا وأنتقدّها معك.

## ماذا يحدث تقنياً خلف الكواليس؟
| الجزء | دوره |
|---|---|
| `blender_bridge_addon.py` | إضافة Blender — تشغّل جسر HTTP تلقائياً (127.0.0.1:9876) |
| `arena_sync.py` | وكيل مزامنة — يسحب أوامري كل 10 ثوانٍ، ينفذها عبر الجسر، يرفع النتائج والصور |
| `arena-bridge/commands/*.json` | الأوامر (أكتبها أنا من المحادثة) |
| `arena-bridge/results/<id>/` | النتائج — result.json + image.png (أقرؤها وأعرضها عليك) |

| أمر | ماذا يفعل في بلندر |
|---|---|
| `health` | فحص الاتصال ونسخة بلندر |
| `scene` | أقرأ مشهدك كاملاً (كائنات/مواد/كاميرات) |
| `exec` | أبني وأعدّل — تراه يحدث LIVE في النافذة |
| `screenshot` | لقطة سريعة أتحقق منها قبل الرفع |
| `render` | رندر نهائي بالجودة التي تحددها |

## حل المشاكل
| المشكلة | الحل |
|---|---|
| الوكيل يطبع «waiting for Blender to open» | عادي — افتح Blender وسينفذ فوراً (الإضافة تعمل تلقائياً) |
| لم تظهر إضافة الجسر بعد التثبيت | ابحث في قائمة Add-ons عن «Arena» وتأكد من تفعيلها |
| الوكيل يقول GitHub رفض الرفع | سجّل دخول GitHub عند أول رفع (مرة واحدة) |
| المنفذ 9876 مشغول | نسختان من بلندر مفتوحتان — أغلق واحدة (أو غيّر المنفذ من إعدادات الإضافة) |
| أريد إيقاف الربط مؤقتاً | أغلق نافذة الوكيل — فقط |

## ملاحظات
- 🔒 الجسر محلي 100% (127.0.0.1) — لا أحد من الشبكة يصل إليه
- 🌐 الربط معي يمر عبر GitHub (يحتاج إنترنت المحادثة) — بينما نسخة n8n+Ollama المحلية تعمل أوفلاين بلا وسط
- 🔓 المستودع عام: صور التصاميم تظهر فيه — للمشاريع الحساسة اجعله خاصاً
