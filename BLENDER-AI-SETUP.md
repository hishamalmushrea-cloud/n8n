# 🪟 ربط n8n بالذكاء الاصطناعي و Blender — دليل كامل

هذه الحزمة تجعل جهازك **استوديو تصميم بالذكاء الاصطناعي يعمل بدون إنترنت**:

```
┌─────────────┐    رسالتك بالعربية    ┌──────────────┐    أدوات     ┌──────────────┐
│    أنت      │ ───────────────────▶ │  n8n + وكيل  │ ──────────▶ │   Blender    │
│ (نافذة محادثة)│                      │  ذكاء اصطناعي │ ◀────────── │  (التصميم)   │
└─────────────┘◀───────────────────└──────────────┘    النتيجة    └──────────────┘
                                        ▲
                                        │ نموذج محلي (Ollama) — لا إنترنت
```

تكتب بالعربية: «صمم غرفة فيها طاولة وكوب ماء ثم رندرها» → الذكاء الاصطناعي يقرأ المشهد،
يكتب كود Python لمكتبة bpy، ينفذه داخل Blender، ويصيّر الصورة النهائية.

## الملفات
| الملف | الوظيفة |
|---|---|
| `blender-bridge/blender_n8n_server.py` | خادم الجسر — يعمل داخل Blender |
| `blender-bridge/n8n-workflow-blender-ai-agent.json` | سير العمل الجاهز للاستيراد في n8n |

---

## الخطوة 1: تشغيل الجسر داخل Blender

1. افتح Blender (أي إصدار حديث 3.x / 4.x)
2. من الأعلى اختر تبويب **Scripting**
3. اضغط **Open** واختر ملف `blender_n8n_server.py`
4. اضغط زر **▶ Run Script**
5. سترى في الكونسول:
   `Blender ↔ n8n Bridge is RUNNING — http://127.0.0.1:9876`

> اترك Blender مفتوحاً أثناء العمل. لتشغيله تلقائياً مع كل فتح لملف: من محرر النصوص فعّل خيار **Register** ثم احفظ الملف داخل ملف الـ blend.

اختبار سريع: افتج في متصفحك `http://127.0.0.1:9876/health` — يجب أن ترى `{"ok": true, ...}`

## الخطوة 2: استيراد سير العمل في n8n

1. شغّل n8n: انقر مرتين على `start-n8n.cmd` (أو `node n8n\bin\n8n start`)
2. افتح **http://localhost:5678** وسجّل الدخول
3. من القائمة العلوية: **Workflows ← Import from File**
4. اختر `blender-bridge/n8n-workflow-blender-ai-agent.json`

## الخطوة 3: تجهيز الذكاء الاصطناعي

### الخيار أ) Ollama — يعمل أوفلاين بلا إنترنت (موصى به)
> تحتاج إنترنت مرة واحدة فقط لتنزيله، وبعدها يعمل دائماً بدون إنترنت:

1. نزّل Ollama لويندوز من `https://ollama.com/download` وثبّته (~200MB)
2. بينما الإنترنت متاح، نزّل نموذجاً يفهم العربية والبرمجة:
   ```cmd
   ollama pull qwen2.5:7b
   ```
   (~4.7GB — إن كان جهازك ضعيفاً استخدم `qwen2.5:3b` بدلاً منه)
3. في n8n: افتح عقدة **Ollama Chat Model** في السير المستورد:
   - اضغط على Credential ← **Create New**
   - Base URL: `http://127.0.0.1:11434`
   - احفظ
4. تأكد أن اسم النموذج في العقدة يطابق ما نزّلته (`qwen2.5:7b`)

### الخيار ب) OpenAI / Anthropic / Gemini (تحتاج إنترنت + مفتاح API)
احذف عقدة Ollama، أضف عقدة Chat Model من المزوّد الذي تريده، اربطها بالوكيل، وضع مفتاحك.

## الخطوة 4: التشغيل 🚀

1. في أسفل الشاشة اضغط أيقونة **Chat** (نافذة المحادثة)
2. اكتب طلبك بالعربية، أمثلة حقيقية للتجربة:
   - «اعرض لي ما يوجد في المشهد»
   - «أنشئ مكعباً أحمر بحجم مترين وطبّقه subdivision»
   - «صمم طاولة خشبية بأربع أرجل فوقها كوب زجاجي»
   - «أضف إضاءة استوديو ثلاثية النقاط ثم رندر المشهد إلى D:/designs/room.png»
   - «حرّك الكاميرا دورة كاملة حول المجسم على 60 إطاراً»
3. الذكاء الاصطناعي سيرى المشهد (أداة Scene)، يكتب وينفذ الكود (أداة Exec)، ويخبرك بالنتيجة

## كيف يعمل الجسر؟ (مرجع سريع)
| Endpoint | الوظيفة |
|---|---|
| `GET /health` | حالة الخادم ونسخة Blender |
| `GET /scene` | كل كائنات المشهد (نوع/موقع/مقاس/مواد/كاميرات) |
| `POST /exec {"code": "..."}` | تنفيذ كود Python داخل Blender — ضع النتيجة في `RESULT` |
| `POST /render {...}` | رندر بجودة كاملة (engine/samples/dimension/camera) |
| `POST /screenshot {...}` | رندر سريع يعود base64 — «عين» الذكاء الاصطناعي |

يمكنك أيضاً بناء سير عمل خاص بك يستدعي هذه العناوين بعقدة **HTTP Request** العادية بدون ذكاء اصطناعي (أتمتة رندر دفعة واحدة مثلاً).

## حل المشاكل
| المشكلة | الحل |
|---|---|
| `ECONNREFUSED 127.0.0.1:9876` | شغّل السكربت داخل Blender أولاً (الخطوة 1) |
| المنفذ 9876 مشغول | شغّل السكربت بمنفذ آخر: عدّل `DEFAULT_PORT` أو مرر `-- --port 9877` في وضع background، وحدّث روابط العقدتين في السير |
| الأداة ترجع timeout | الرندر الطويل يرفع المهلة: أضف `"timeout": 3600` في جسم الطلب |
| Ollama لا يتصل | `ollama serve` يجب أن يعمل؛ جرّب `http://127.0.0.1:11434` في المتصفح |
| الكود فشل | الوكيل مصمم ليقرأ الخطأ ويصحح الكود تلقائياً — أخبره بالمشكلة إن تكررت |
| نافذة ويندوز تطلب سماح Firewall | اختر Allow — الاتصال محلي فقط (127.0.0.1) |

## ملاحظة أمان 🔒
الجسر يستمع على `127.0.0.1` فقط = لا يمكن لأي جهاز آخر على الشبكة الوصول إليه.
لكن أي عملية على جهازك تستطيع تنفيذ كود عبره — لا تفتح المنفذ على الشبكة.
